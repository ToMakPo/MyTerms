<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myterms">

    <application
        android:name=".application.App"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".dashboard.DashboardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".term.TermViewActivity" />
        <activity
            android:name=".term.TermEditActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".term.TermSelectActivity" />
        <activity android:name=".course.CourseViewActivity" />
        <activity
            android:name=".course.CourseEditActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".mentor.MentorSelectActivity" />
        <activity
            android:name=".mentor.MentorEditActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".assessment.AssessmentEditActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".note.NoteEditActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".tracker.TrackerActivity" />
        <activity android:name=".term.TermListActivity" />

        <receiver
            android:name=".notifications.Alarm"
            android:enabled="true"
            android:exported="true" />
    </application>

</manifest>